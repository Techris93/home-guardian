# Home Guardian — Environment Variables
# Copy to .env and fill in before starting the server.

# Required: generate with: openssl rand -hex 16
HOME_GUARDIAN_API_KEY=your-secure-key-here

# Optional: restrict CORS to specific origins (comma-separated)
HOME_GUARDIAN_CORS_ORIGINS=http://localhost:5051
